import 'package:flutter/material.dart';

class RequestManagement extends StatelessWidget {
  const RequestManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        Card(child: SizedBox(height: 360)),
        Card(child: SizedBox(height: 360)),
        Card(child: SizedBox(height: 360)),
      ],
    );
  }
}
